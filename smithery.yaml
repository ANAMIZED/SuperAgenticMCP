startCommand:
  type: stdio
  configSchema:
    type: object
    required: []
    properties: {}
  exampleConfig: {}
  commandFunction: |
    (config) => ({
      command: 'python',
      args: ['-m', 'superagenticmcp.server']
    })
