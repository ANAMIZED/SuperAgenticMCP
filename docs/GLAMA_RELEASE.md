# Glama admin — SuperAgenticMCP

Sync Server. Pinned SHA empty.
Build: `["python3 -m pip install --break-system-packages --no-cache-dir ."]`
CMD: `["python3", "-m", "superagenticmcp.server"]`
Env schema: `{"type":"object","properties":{},"required":[]}`
Placeholders: `{}`
