# SuperAgenticMCP docs

Human-oriented documentation lives here.

- Architecture and routing model — see root `README.md` and `AGENTS.md`
- Interactive design prototype — root `superagenticmcp.html`
- Agent skill surface — root `SKILL.md`

As the runtime solidifies, expand this directory with:

- Architecture deep-dive
- Config reference (`superagentic.json`)
- Board protocol / JSONL schema
- Memory-scope data model
